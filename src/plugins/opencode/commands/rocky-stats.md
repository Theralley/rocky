---
description: Show rocky lifetime token-savings stats
---
Show rocky stats — total tokens saved, sessions, average compression ratio.

Read the lifetime history log at `~/.config/rocky/.rocky-history.jsonl`
(or wherever the rocky-stats script writes it). Output: total saved,
sessions counted, avg ratio. One short table.
